import ConfigLoader from "./ConfigLoader";
import ProviderFactory from "./ProviderFactory"

export const TasitProviderHelpers = {
  ProviderFactory,
  ConfigLoader,
};

export default TasitProviderHelpers;
